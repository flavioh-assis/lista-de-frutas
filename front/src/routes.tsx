import { BrowserRouter, Routes as Switch, Route } from 'react-router-dom';

const Routes = () => (
  <BrowserRouter>
    <Switch>
      <Route
        path='/'
        element
      />
    </Switch>
  </BrowserRouter>
);

export default Routes;
